runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
 python_version: 3.7

# runtime: python310

# entrypoint: gunicorn -b :$PORT main:app

# handlers:
# - url: /.*
#   script: auto
